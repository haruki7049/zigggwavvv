name: Ziglang CI

on:
  push:
  pull_request:

jobs:
  run-zig-build-test:
    strategy:
      matrix:
        os: [ubuntu-latest, macOS-latest, windows-latest]
    runs-on: ${{matrix.os}}
    steps:
      - uses: actions/checkout@v6.0.3
      - uses: mlugg/setup-zig@v2.2.1
        with:
          version: 0.15.2
      - run: zig build test
